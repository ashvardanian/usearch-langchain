# USearch + LangChain Basic Examples

This repository contains basic examples of how to use the USearch and LangChain APIs.

## Quickstart

Setup the environment:

```bash
git clone
npm install --force
```


Run the example:

```bash
node main.js
```